{
  "name": "github-stack-builders-app",
  "version": "1.0.0",
  "description": "A TypeScript application for exploring Stack Builders GitHub repositories",
  "main": "dist/app.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "npm run build && node -e \"console.log('Build complete. Open index.html in your browser.')\"",
    "test": "npm run build && node dist/tests.js",
    "clean": "rm -rf dist"
  },
  "keywords": ["github", "api", "typescript", "functional-programming", "bdd"],
  "author": "Mao Astuillo",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
